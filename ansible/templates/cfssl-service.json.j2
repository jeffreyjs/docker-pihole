{
    "CN": "{{ certificate_cn }}",
    "key": {{ cfssl_ca.key | to_nice_json }},
    "names": {{ cfssl_ca.names | to_nice_json }},0
    "hosts": [
        "{{ certificate_cn }}",
        "localhost"
    ]
}
